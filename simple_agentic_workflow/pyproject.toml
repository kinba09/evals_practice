[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "simple-agentic-workflow"
version = "0.1.0"
description = "A small single-agent LangGraph workflow with deterministic tools"
requires-python = ">=3.11"
dependencies = [
    "langchain-core>=0.3,<2.0",
    "langchain>=1.0,<2.0",
    "langchain-openai>=0.3.9,<2.0",
    "langgraph>=0.2,<2.0",
    "python-dotenv>=1.0,<2.0",
    "deepeval>=4.1,<5.0",
    "langfuse>=3.0,<5.0",
    "pytest>=8.0,<9.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "agent_eval: evaluation tests that call the configured LLM gateway",
]
